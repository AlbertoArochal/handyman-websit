---
interface Props {
	title: string;
	description?: string;
}

const { title, description = "Servicios profesionales de mantenimiento del hogar" } = Astro.props;
---

<!doctype html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content={description} />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<title>{title} | ReyrLesa Reformas</title>
		<style is:global>
			* {
				margin: 0;
				padding: 0;
				box-sizing: border-box;
			}

			:root {
				--primary-color: #2563eb;
				--primary-dark: #1e40af;
				--secondary-color: #f97316;
				--dark-bg: #1f2937;
				--light-bg: #f9fafb;
				--text-dark: #111827;
				--text-light: #6b7280;
				--border-color: #e5e7eb;
				--card-bg: #ffffff;
				--input-bg: #ffffff;
			}

			/* Modo oscuro solo en móvil cuando el usuario tiene preferencia de tema oscuro */
			@media (max-width: 768px) and (prefers-color-scheme: dark) {
				:root {
					--primary-color: #3b82f6;
					--primary-dark: #2563eb;
					--secondary-color: #fb923c;
					--dark-bg: #0f172a;
					--light-bg: #1e293b;
					--text-dark: #f1f5f9;
					--text-light: #94a3b8;
					--border-color: #334155;
					--card-bg: #1e293b;
					--input-bg: #0f172a;
				}

				html {
					background-color: var(--dark-bg);
					color-scheme: dark;
				}

				h1, h2, h3, h4 {
					color: var(--text-dark);
				}

				a {
					color: var(--primary-color);
				}

				a:hover {
					color: var(--secondary-color);
				}
			}

			html {
				font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
				background-color: var(--light-bg);
				color: var(--text-dark);
			}

			body {
				line-height: 1.6;
				color: var(--text-dark);
			}

			a {
				color: var(--primary-color);
				text-decoration: none;
				transition: color 0.3s ease;
			}

			a:hover {
				color: var(--primary-dark);
			}

			button {
				cursor: pointer;
				border: none;
				border-radius: 0.5rem;
				padding: 0.75rem 1.5rem;
				font-size: 1rem;
				transition: all 0.3s ease;
			}

			.btn-primary {
				background-color: var(--primary-color);
				color: white;
			}

			.btn-primary:hover {
				background-color: var(--primary-dark);
				transform: translateY(-2px);
				box-shadow: 0 4px 12px rgba(37, 99, 235, 0.3);
			}

			.btn-secondary {
				background-color: var(--secondary-color);
				color: white;
			}

			.btn-secondary:hover {
				background-color: #ea580c;
				transform: translateY(-2px);
				box-shadow: 0 4px 12px rgba(249, 115, 22, 0.3);
			}

			.container {
				max-width: 1200px;
				margin: 0 auto;
				padding: 0 1rem;
			}

			section {
				padding: 3rem 0;
			}

			h1, h2, h3 {
				margin-bottom: 1rem;
				font-weight: 700;
			}

			h1 {
				font-size: 2.5rem;
			}

			h2 {
				font-size: 2rem;
			}

			h3 {
				font-size: 1.5rem;
			}

			p {
				margin-bottom: 1rem;
				color: var(--text-light);
				line-height: 1.8;
			}
		</style>
	</head>
	<body>
		<nav class="navbar">
			<div class="container nav-container">
				<div class="logo">
					<a href="/">ReyrLesa</a>
				</div>
				<input type="checkbox" id="menu-toggle" class="menu-toggle">
				<label for="menu-toggle" class="menu-icon">
					<span></span>
					<span></span>
					<span></span>
				</label>
				<ul class="nav-links">
					<li><a href="/">Inicio</a></li>
					<li><a href="/services">Servicios</a></li>
					<li><a href="/about">Nosotros</a></li>
					<li><a href="/contact">Contacto</a></li>
				</ul>
			</div>
		</nav>

		<main>
			<slot />
		</main>

		<footer class="footer">
			<div class="container">
				<div class="footer-content">
					<div class="footer-section">
						<h4>ReyrLesa Reformas</h4>
						<p>Tu socio de confianza para todas tus reformas y reparaciones</p>
					</div>
					<div class="footer-section">
						<h4>Enlaces Rápidos</h4>
						<ul>
							<li><a href="/services">Servicios</a></li>
							<li><a href="/about">Sobre Nosotros</a></li>
							<li><a href="/contact">Contacto</a></li>
						</ul>
					</div>
					<div class="footer-section">
						<h4>Información de Contacto</h4>
						<p>Teléfono: 613 33 44 55</p>
						<p>Email: reycoc@gmail.com</p>
					</div>
				</div>
				<div class="footer-localities">
					<h4>Reparaciones y Reformas en el Sur de Tenerife</h4>
					<div class="localities-grid">
						<a href="/">Reparaciones y reformas Adeje</a>
						<a href="/">Reparaciones y reformas Arona</a>
						<a href="/">Reparaciones y reformas Los Cristianos</a>
						<a href="/">Reparaciones y reformas Playa de las Américas</a>
						<a href="/">Reparaciones y reformas Costa Adeje</a>
						<a href="/">Reparaciones y reformas Granadilla de Abona</a>
						<a href="/">Reparaciones y reformas San Miguel de Abona</a>
						<a href="/">Reparaciones y reformas El Médano</a>
						<a href="/">Reparaciones y reformas Las Galletas</a>
						<a href="/">Reparaciones y reformas Palm-Mar</a>
						<a href="/">Reparaciones y reformas Guía de Isora</a>
						<a href="/">Reparaciones y reformas Santiago del Teide</a>
					</div>
				</div>
				<div class="footer-bottom">
					<p>&copy; 2026 ReyrLesa Reformas. Todos los derechos reservados.</p>
				</div>
			</div>
		</footer>

		<style>
			.navbar {
				background-color: var(--dark-bg);
				color: white;
				padding: 1rem 0;
				position: sticky;
				top: 0;
				z-index: 100;
				box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
			}

			.nav-container {
				display: flex;
				justify-content: space-between;
				align-items: center;
			}

			.logo a {
				font-size: 1.8rem;
				font-weight: 800;
				color: var(--secondary-color);
			}

			.nav-links {
				display: flex;
				list-style: none;
				gap: 2rem;
			}

			.nav-links a {
				color: white;
				transition: color 0.3s ease;
			}

			.nav-links a:hover {
				color: var(--secondary-color);
			}

			/* Hamburger menu */
			.menu-toggle {
				display: none;
			}

			.menu-icon {
				display: none;
				flex-direction: column;
				cursor: pointer;
				gap: 5px;
				padding: 5px;
			}

			.menu-icon span {
				display: block;
				width: 25px;
				height: 3px;
				background-color: white;
				border-radius: 2px;
				transition: all 0.3s ease;
			}

			.footer {
				background-color: var(--dark-bg);
				color: white;
				padding: 3rem 0 1rem;
				margin-top: 4rem;
			}

			.footer-content {
				display: grid;
				grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
				gap: 2rem;
				margin-bottom: 2rem;
			}

			.footer-section h4 {
				color: var(--secondary-color);
				margin-bottom: 1rem;
			}

			.footer-section ul {
				list-style: none;
			}

			.footer-section ul li {
				margin-bottom: 0.5rem;
			}

			.footer-section a {
				color: #d1d5db;
			}

			.footer-section a:hover {
				color: var(--secondary-color);
			}

			.footer-bottom {
				border-top: 1px solid var(--border-color);
				padding-top: 1rem;
				text-align: center;
				color: #9ca3af;
			}

			.footer-localities {
				margin-top: 2rem;
				padding: 2rem 0 2.5rem;
				border-top: 1px solid #374151;
			}

			.footer-localities h4 {
				color: var(--secondary-color);
				margin-bottom: 1.5rem;
				text-align: center;
			}

			.localities-grid {
				display: grid;
				grid-template-columns: repeat(3, 1fr);
				gap: 0.75rem 2rem;
				max-width: 900px;
				margin: 0 auto;
			}

			.localities-grid a {
				color: #d1d5db;
				font-size: 0.875rem;
				font-weight: 600;
				transition: all 0.3s ease;
				position: relative;
				padding-left: 1.25rem;
			}

			.localities-grid a::before {
				content: "→";
				position: absolute;
				left: 0;
				color: var(--secondary-color);
				transition: transform 0.3s ease;
			}

			.localities-grid a:hover {
				color: var(--secondary-color);
			}

			.localities-grid a:hover::before {
				transform: translateX(3px);
			}

			@media (max-width: 600px) {
				.localities-grid {
					grid-template-columns: 1fr;
					text-align: center;
				}
			}

			@media (max-width: 768px) {
				.nav-container {
					flex-wrap: wrap;
				}

				.menu-icon {
					display: flex;
				}

				.nav-links {
					display: none;
					flex-direction: column;
					width: 100%;
					text-align: center;
					padding: 1rem 0;
					gap: 0;
				}

				.nav-links li {
					padding: 0.75rem 0;
					border-top: 1px solid #374151;
				}

				.menu-toggle:checked ~ .nav-links {
					display: flex;
				}

				.menu-toggle:checked ~ .menu-icon span:nth-child(1) {
					transform: rotate(45deg) translate(5px, 5px);
				}

				.menu-toggle:checked ~ .menu-icon span:nth-child(2) {
					opacity: 0;
				}

				.menu-toggle:checked ~ .menu-icon span:nth-child(3) {
					transform: rotate(-45deg) translate(6px, -6px);
				}

				h1 {
					font-size: 1.8rem;
				}

				h2 {
					font-size: 1.5rem;
				}

				.footer-content {
					text-align: center;
				}

				.footer-section ul {
					padding: 0;
				}
			}
		</style>
	</body>
</html>
